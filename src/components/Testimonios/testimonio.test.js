import React from 'react';
import { render } from '@testing-library/react';
import Testimonios from './Testimonios';

test('renderiza el componente Testimonios', () => {
  render(<Testimonios />);
});
